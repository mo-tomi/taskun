# pnpm設定ファイル
# パッケージの重複を避けるためのホイスト設定
auto-install-peers=true
# より高速なインストールのための並列設定
prefer-frozen-lockfile=false
# パッケージのバージョン管理を厳密に
exact=false
# パッケージの保存場所を最適化
store-dir=.pnpm
# TypeScriptプロジェクトでの型定義自動インストール
auto-install-types=true
